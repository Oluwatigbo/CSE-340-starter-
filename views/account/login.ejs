<main>
  <h1>Login</h1>
  <% if (message) { %><p><%= message %></p><% } %>
  <% if (errors && errors.length > 0) { %>
    <ul><% errors.forEach(e => { %><li><%= e %></li><% }) %></ul>
  <% } %>
  <form action="/account/login" method="POST">
    <label for="account_email">Email:</label>
    <input type="email" id="account_email" name="account_email" required value="<%= typeof account_email !== 'undefined' ? account_email : '' %>" />
    <label for="account_password">Password:</label>
    <input type="password" id="account_password" name="account_password" required />
    <button type="submit">Login</button>
  </form>
  <p><a href="/account/register">Register</a></p> <!-- If registration exists -->
</main>